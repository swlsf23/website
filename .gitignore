# Dependencies
node_modules/
venv/
.venv/

# Build / cache
dist/
dist-ssr/
*.local
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets (keep .env.example if you add one)
.env
.env.*
!.env.example

# Résumé phone/email (see personal/resume.contact.example.md)
personal/resume.contact.local.md

# Generated PDFs (contact baked in; build before deploy — see scripts/render_resume_pdf.mjs)
apps/web/public/resume.pdf
apps/web/public/writing-samples.pdf

# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
infra/terraform/terraform.tfvars

# OS / editors
.DS_Store
.idea/
*.sw?
